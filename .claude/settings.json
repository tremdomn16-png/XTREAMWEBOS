{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.ts|*.tsx) npx eslint \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "ESLint no arquivo editado..."
          }
        ]
      }
    ]
  }
}
